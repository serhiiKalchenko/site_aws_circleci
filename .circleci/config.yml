version: 2.1
jobs:
  CI_partition:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      # job steps
  CD_partition:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      # job steps

workflows:
  my_workflow:
    jobs:
      - CI_partition:
      - CD_partition:
          requires:
            - CI_partition:
